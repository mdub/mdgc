---
export interface Props {
  quote?: string;
}

const { quote } = Astro.props;
---

<div class="bg-blue-50 rounded-lg p-8 mb-8">
  <h2 class="text-2xl font-bold text-mdgc-blue-dark mb-4">Course Overview</h2>
  {quote && (
    <p class="text-mdgc-blue text-lg mb-4">
      "{quote}"
    </p>
  )}
  <div class="text-gray-600">
    <slot />
  </div>
</div>