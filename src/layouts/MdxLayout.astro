---
import Layout from './Layout.astro';

const { frontmatter } = Astro.props;
---

<Layout title={frontmatter.title} subtitle={frontmatter.subtitle} cmsPath={frontmatter.cmsPath}>
	<slot />
</Layout>
