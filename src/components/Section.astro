---
export interface Props {
	title?: string;
	class?: string;
}

const { title, class: className = "" } = Astro.props;
---

<section class={`my-12 ${className}`}>
	{title && (
		<h2 class="text-2xl font-semibold mb-6">
			{title}
		</h2>
	)}
	<slot />
</section>
